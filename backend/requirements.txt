# ── Core Web Framework ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── ORM & Database ──────────────────────────────────────────────────────────
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9     # Driver PostgreSQL (producción)
alembic>=1.13.1             # Migraciones

# ── Validación de datos ─────────────────────────────────────────────────────
pydantic[email]>=2.7.0      # Incluye email-validator para EmailStr

# ── Seguridad / Hashing ─────────────────────────────────────────────────────
bcrypt>=4.0.0               # Hashing bcrypt directamente (passlib no soporta bcrypt>=4)

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0               # Cliente HTTP para tests de integración futuros
